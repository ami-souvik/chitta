{
  "name": "chitta",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/web",
    "apps/api",
    "infra"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "deploy:infra": "cd infra && npx cdk deploy",
    "deploy:api": "./scripts/deploy-api.sh"
  },
  "devDependencies": {
    "turbo": "^2.9.16"
  }
}
